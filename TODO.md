# TODO

## 總覽

本清單將對話審計、指揮鏈席位化、介面可視化、L0 環境／記憶核心重構、後端降 LLM 倚賴、DeepSeek Club 插件／[dsh-plugin](https://github.com/topics/dsh-plugin)／世界模組整合，以及 **Minecraft 適配器／編譯器** 等需求**全面整合**為同一契約與驗收面。共同原則：

- **顯式動作優先**：審計、暫停／恢復、L0 刷新、插件安裝／啟用、編譯／部署等皆為用戶或 API 觸發的按需動作；禁止在串流或聊天完成時自動灌入分數／數字，亦禁止自動安裝／啟用外掛或自動部署編譯產物。
- **契約一致**：前後端狀態機、席位表、預算、模組閘道與 `docs/company/roles.md` 對齊；UI 只反映契約狀態，不自行推導審計結果或繞過協調器推斷席位／工具權限。
- **指揮鏈與護欄不可鬆綁**：工作項只經協調器下發；L0 無工具直連；MCP／OPC／Minecraft 寫入與插件呼叫皆須經既有護欄與白名單；禁止業務代碼、上層席位或外掛繞過指揮鏈。
- **可觀測、可降級**：注入失敗、快取未命中、配額超限、外掛外連失敗、編譯失敗皆須可診斷並有明確降級路徑（規則／快取／停用外掛／保留舊版本／暫停依策略）。
- **測試隔離**：短路、快取、啟發式、審計流程、模組／插件／編譯路徑須可 `monkeypatch`／stub，不依賴真實大模型 API、真實外掛遠端或真實遊戲伺服器。

### 與三大模組拆分對照

| 產品模組 | 主要承接章節 | 覆蓋度 | 契約要點 |
| --- | --- | --- | --- |
| **敘事核心** | §4（L0 三核＋§4.7 邊界）、§2（敘事席／指揮鏈） | ✅ 已納入 | 世界觀單一真相來源＝L0／憲法／實體 store；敘事不得雙寫圖譜；產出經顯式寫入 API |
| **RPG／GM 控制台** | §3（可視化）、§1（審計入口）、§9（狀態） | ✅ 已納入 | 預設折疊防過載；審計不佔常駐分數槽；狀態以後端矩陣為準 |
| **MC 適配器／編譯器** | §8、§7.1／§7.2 相關驗收 | ✅ 已納入 | 編譯／部署顯式；沙盒過才待部署；失敗保留舊版、禁自動 `/reload` |

> 本清單仍以「指揮鏈治理＋L0 認知＋插件生態＋MC 編譯」為契約層；三大產品模組是其上的產品切片，不得另開第二套狀態機。
>
> 適用範圍：當前契約假設**單用戶／單租戶**；多租戶隔離策略（席位表、L0 圖譜、狀態矩陣是否每租戶獨立）另文定義，實作時不得預設共享或隔離。

### 實施優先級（破壞性排序）

| 優先級 | 章節 | 理由 |
| --- | --- | --- |
| **P0** | §2 指揮鏈席位化 | 其餘契約的骨架；不先落地，可視化／審計／插件無處掛載 |
| **P0** | §1 對話審計契約 | 最易被「順手自動灌入」破壞，須最早立規矩 |
| **P0** | §8 MC 適配器／編譯器契約 | 差異化能力；須與「顯式動作優先」同級納入治理 |
| **P1** | §4 L0 三核＋敘事邊界 | 認知層／敘事輸入源；越早穩定越好 |
| **P1** | §5 降 LLM 依賴 | 直接影響成本；與 §1／§4 耦合 |
| **P1** | §9 狀態組合矩陣 | 暫停／審計／刷新／降級疊加規則，避免實作各自解讀 |
| **P2** | §3 可視化 | 依賴 §2／§4／§9 狀態機穩定後再做，否則 UI 反覆改 |
| **P2** | §6 插件整合 | 依賴 §2 席位表與 §5 路由體系，最後接入 |

> 建議後續將本檔「禁止／必須」條款抽出為獨立 `CONTRACT.md`（或 `docs/contracts/runtime.md`），並對應 CI 靜態檢查／pytest 不變式；本清單每條 `[ ]` 應能映射到測試或 lint，而非僅文件層面。

---

## 1. 對話審計（按需觸發 + 暫停後審計）　`P0`

### 1.1 功能定位

對話審計是**顯式、按需**的品質／合規檢查流程，用於在用戶主動要求時，對當前對話／任務上下文產出結構化審計結果（維度說明、風險點、建議），**不得**作為常駐 HUD 數字或串流附帶分數；插件、L0、降 LLM 路徑、編譯器皆不得改寫此契約。

### 1.2 UI／交互行為

- [ ] **禁止自動展示分數／數字**：對話介面、串流完成回調、任務結束事件、模組 webhook 皆不得自動寫入或渲染審計分數、百分比或等級徽章。
- [ ] **點擊「審計」才觸發**：提供明確入口（工具列按鈕、訊息選單或指令），用戶點擊後才進入審計流程並展示結果面板／報告。
- [ ] **結果展示形態**：以可讀報告為主（維度說明、依據摘要、建議動作），分數若存在僅作為報告內可選欄位，且僅在審計完成後出現；與預算剩餘額度、壓力指標、外掛狀態等其他數字嚴格分離，避免混淆。

### 1.3 觸發時序：完成當前請求 → 暫停 → 審計

- [ ] 用戶點擊「審計」時，**先等待當前進行中的請求（inflight）自然完成**，不得強制 abort／kill 串流。
- [ ] 當前請求完成後，任務進入顯式 **`paused`** 狀態（可恢復），再啟動審計流程。
- [ ] 完整狀態序列：`running`（等待 inflight 結束）→ `paused` → `auditing` → 展示結果 → 用戶可選擇恢復（`resume`）或保持暫停。
- [ ] 暫停為**狀態機狀態**，非強制中斷；恢復 API 須與 UI 狀態一致（按鈕可用性、席位狀態文案同步）。

### 1.4 前後端契約

- [ ] 審計為顯式動作 API（例如 `POST /audit` 或等效 RPC），請求體攜帶對話／任務 ID 與可選範圍；回應為審計報告結構，非串流 token 附帶欄位。
- [ ] 暫停／恢復 API 與審計解耦但可編排：審計編排層呼叫「等待 inflight → pause → run audit」；UI 訂閱同一狀態通道。
- [ ] **禁止**在聊天完成 webhook、串流 `done` 事件、插件回調中自動灌入審計分數或觸發審計。
- [ ] 錯誤與超時：審計失敗須回傳可觀測錯誤碼與可重試提示；**預設保持 `paused`**，由用戶顯式選擇「恢復」或「放棄」；若採回滾策略須在 API／文件中寫死，不得靜默寫入假分數。

### 1.5 暫停—恢復一致性（補強）

- [ ] 進入 `paused` 時凍結**上下文版本號**（含：對話快照 ID、L0 注入快照 ID、席位表 `schema_version`、已啟用插件集合雜湊）。
- [ ] **插件集合雜湊輸入（寫死）**：雜湊輸入＝每個插件的「插件 ID＋pin 版本＋啟用狀態」；任一變化（含僅 pin 版本升級、啟停狀態不變）皆視為快照變更，恢復時須經衝突確認（§9 `awaiting_confirmation`）。
- [ ] 恢復（`resume`）前必須校驗版本；若 L0 已刷新、席位表已變更或插件集合已變，**禁止靜默續跑**：回傳衝突錯誤碼，要求用戶確認「以新上下文重綁」或「放棄任務」。
- [ ] 審計過程中禁止並行觸發會改變凍結快照的動作（見 §9）；允許佇列化待用戶確認的刷新請求。

### 1.6 審計軌跡持久化（補強）

- [ ] **軌跡存儲**：審計軌跡（含插件呼叫記錄、編譯／部署呼叫記錄，見 §6.3／§8.4）與對話記錄**同級持久化存儲**。
- [ ] **保留策略**：預設與對話歷史一致（可配置）；插件降級／停用不得丟失已記錄軌跡（對齊 §6.3「降級不丟日誌」）。
- [ ] **軌跡只讀**：審計流程**只讀軌跡、不寫軌跡**；軌跡自身不可被審計或插件修改，僅能經顯式管理動作清理（保留期到期／管理員清除），清理動作本身須可審計。

---

## 2. 指揮鏈角色獨立化（可配置席位）　`P0`

### 2.1 功能定位

將公司運行時整套指揮鏈從「耦合在單一流程裡的隱式步驟」拆成**可配置角色／席位**，由協調器按 DAG／策略調度，使職責、Prompt、權限邊界可獨立演進與觀測；世界模組／DeepSeek Club／dsh-plugin 僅能作為席位可呼叫能力的白名單來源，不得另開隱式指揮鏈。

### 2.2 席位與層級

- [ ] 獨立席位至少涵蓋：
  - **L5→L4→L3→L2**：策略／分解／執行編排鏈（名稱與職責以 `docs/company/roles.md` 為準）
  - **獨立 L1 憲兵**：合規／安全閘門，可攔截或要求修正，不承擔業務執行
  - **L0 注入席**：僅環境與記憶注入（詳見 §4），不執行任務、不直連工具
  - 既有 **RAHO、Reviewer、Synthesizer** 等與指揮鏈對齊為同級或掛接席位，避免雙軌隱式邏輯
- [ ] 席位表（配置）欄位至少含：角色 ID、顯示名、職責說明、預設 Prompt／模板引用、可呼叫工具／模組範圍、**可呼叫插件白名單**、上下游邊界、是否可跳過／可並行、**最高可掛載插件來源層級**（若適用）、**是否具備部署確認權**（見 §8.2；預設僅 L5 與人類管理員為 `true`）。

### 2.3 調度與邊界

- [ ] 各層**職責、Prompt、工作項 DAG 邊界清晰**；工作項只經協調器下發，**禁止**業務代碼、上層席位或插件繞過指揮鏈直連下層工具／席位。
- [ ] 協調器負責：選席、傳上下文（含 L0 注入結果）、收集產出、觸發 L1 檢查、預算扣減、模組／插件呼叫編排與狀態更新。
- [ ] 與**預算模組**對齊：每席／每任務／每插件呼叫消耗可歸因；超預算時按策略降級或暫停，而非靜默續跑。
- [ ] 文件同步：席位表變更必須同步更新 `docs/company/roles.md` 與相關 OpenAPI／事件 schema。

### 2.4 版本化、仲裁與插件掛載（補強）

- [ ] 席位表帶 **`schema_version`**；舊任務在席位表變更時進入「凍結」狀態或走強制遷移路徑，禁止用新席位表默認改寫進行中 DAG。
- [ ] **L1 仲裁**：L1 攔截後，若 L3（或執行席）堅持執行，不得由執行席自行推翻；須升級至 **L5 決策或人工干預**（顯式 API／UI），並留下可觀測決策記錄。
- [ ] 插件掛載須聲明**最高可掛載席位層級**（例：部分插件僅允許掛 L2 以下）；來源標籤（`deepseek-club`／`dsh-plugin`）可參與策略，但不得繞過此上限。

### 2.5 驗收要點

- [ ] 可在配置中開關或替換單一席位 Prompt／模型路由，而不改編排主流程代碼。
- [ ] 繞過指揮鏈的直連路徑（含插件捷徑）在 CI／靜態檢查或運行時守衛中可被偵測並拒絕。

---

## 3. 對話介面：角色／預算／狀態／動作可視化　`P2`

### 3.1 功能定位

在對話／控制台側提供**即時、可讀**的運行時可視化，讓用戶看清「誰在做事、花了多少、卡在哪、正在做哪類動作、哪些模組已啟用」，且與審計分數展示嚴格分離。對齊「RPG／GM 控制台」資訊架構，但須避免側欄資訊過載。

### 3.2 顯示內容

- [ ] **當前激活角色／席位**：展示指揮鏈 L5→L0、Reviewer／Synthesizer 等當前激活項，文案與圖示與 `docs/company/roles.md`、席位表一致；支援多席並行時的主／從標示。
- [ ] **主／從定義（寫死）**：主席位＝當前持有工作項 DAG **根節點**的席位；從席位＝並行支援／旁支；切換僅能由協調器狀態通道驅動，UI 不得自行推斷。
- [ ] **預算總額與剩餘額度**：與公司運行時預算模組同步；呈現總額、已用、剩餘及可選分席／分模組消耗；狀態清楚可讀（進度條／狀態色），**非僅數字堆疊**；超限或接近上限有明確提示。
- [ ] **席位／任務狀態**：區分 **活動中** vs **等待中**，並涵蓋顯式狀態：`paused`、`auditing`、恢復中、注入刷新中、外掛降級中、編譯中／部署待確認等；狀態來源以後端狀態機為準（見 §9）。
- [ ] **當前執行動作類型**：MCP 呼叫、編程、內容思考、編排／調度、外掛／模組呼叫、編譯／沙盒驗證等，隨階段即時更新；標籤不得與審計分數或壓力百分比混用同一元件。
- [ ] **模組／外掛啟用狀態**（對齊 §6／§7）：可選展示已啟用模組（含 DeepSeek Club、dsh-plugin）與能力摘要，與審計結果區分離。

### 3.3 資訊層級（防過載）

- [ ] **預設折疊策略**：側欄預設只顯示「當前激活席位（主）＋動作類型＋預算進度條」；其餘（分席消耗、壓力細節、模組清單、從席列表）收入可展開面板／下鑽頁。
- [ ] 審計結果不佔用常駐狀態列的「分數」槽位；僅在用戶觸發審計後掛載到結果區。

### 3.4 交互與契約

- [ ] 可視化訂閱任務／席位／模組狀態通道。
- [ ] 暫停／審計／恢復／L0 刷新／插件啟用／編譯部署時，按鈕與狀態文案同步鎖定／解鎖，避免重複提交（對齊 §9）。

---

## 4. L0 環境與記憶核心（重構）＋敘事邊界　`P1`

### 4.1 定位（寫死）

- [ ] **L0 只做環境與記憶注入，不執行任務**。
- [ ] L0 輸出一律經協調器**滲透**至 L1–L5（指揮鏈／憲兵／執行席）；**禁止** L0 直連工具、MCP、插件 API，或繞過協調器下發工作項。
- [ ] L0 注入失敗須**可降級**（例如空注入＋告警）且**可觀測**（錯誤碼、影響層級、是否已降級）。

### 4.2 三核驅動（可觀測、可配置、可單獨開關）

將原「三核」拆成邊界清楚的模組，可獨立診斷與開關：

- [ ] **記憶整理**
  - 會話記憶／長期記憶的壓縮、去重、召回排序
  - 與向量庫、記憶瀏覽器資料模型對齊
  - 輸出：供上層注入的記憶片段列表（含來源、時效、相關度）
- [ ] **知識圖譜**
  - 實體／關係抽取與查詢
  - 圖譜視圖與檢索結果可對照（同一任務子圖）
  - 輸出：與當前任務相關的節點／邊摘要，而非全圖傾倒
- [ ] **態勢偏置**
  - 依當前任務與環境壓力調整注入權重與提示偏置
  - **不代替決策或執行**；只影響「注入什麼／權重多少」
  - 輸出：偏置方向、權重向量、影響層級（L1–L5）說明

### 4.3 三核 DAG 與旁路（補強）

- [ ] 預設管線：**記憶整理 → 知識圖譜 → 態勢偏置**（串行依賴）；態勢偏置可消費前兩者輸出。
- [ ] 允許單獨旁路／開關任一核；旁路時下游須有明確降級輸入（空片段／空子圖／單位偏置），並寫入路由原因碼。
- [ ] 注入前執行**脱敏**：禁止將原始密鑰、憑證、未授權 PII 寫入注入片段；檢索命中含敏感欄位時過濾或遮罩後再注入。

### 4.4 壓力指標

- [ ] 顯示**環境壓力**（預設 **0%**）；定義計算來源（佇列深度、錯誤率、預算緊張度、外部信號、外掛錯誤率等）與更新節奏。
- [ ] **預設等權**歸一化，權重表可配置；壓力變化必須反映到三核**注入強度**（可配置曲線）；禁止 UI 只顯示死數字而注入邏輯不變。
- [ ] 壓力與審計分數分離：壓力屬 L0／環境，審計屬按需報告。

### 4.5 操作面

- [ ] 提供「**重新整理**」顯式動作：重載記憶／圖譜／態勢快照；非背景靜默覆寫。
- [ ] 刷新前後狀態可對照（時間戳、版本／快照 ID、變更摘要）。
- [ ] 刷新進行中時 UI 顯示進行態，避免與任務 `auditing`／`paused`／外掛啟用狀態混淆（見 §9）。

### 4.6 可視化面板（對話／控制台側）

重構為可點進細節的面板：

- [ ] **記憶瀏覽器**：條目列表、來源、時效、是否已注入生效
- [ ] **知識圖譜**：節點／邊摘要與當前任務相關子圖
- [ ] **態勢雷達**：壓力、偏置方向、影響層級（L1–L5）
- [ ] **向量庫**：集合／檢索命中摘要；**禁止**暴露原始密鑰或提供未授權寫入入口

### 4.7 敘事核心與 L0 邊界（補強 · 防雙寫）

- [ ] **世界觀單一真相來源**：進行中劇情／世界觀狀態以 L0 知識圖譜（＋憲法／實體 store）為準；敘事核心（靈境敘事子角色／StoryForge 路徑）**不得**維護第二套可寫世界觀圖譜。
- [ ] 敘事核心讀取世界觀：僅經協調器下發的 **L0 注入結果**（或查詢 API 的唯讀投影），禁止直連向量庫寫入或私自改圖譜。
- [ ] L0 刷新時：若任務處於敘事進行中，須遵守 §1.5／§9——刷新改變快照版本時，進行中劇情進入衝突確認，禁止靜默雙寫導致不一致。
- [ ] 敘事產出若需落庫（章節、任務、NPC 等），一律經既有 `/linkin/*` 或模組閘道的**顯式寫入 API**，並可被審計軌跡引用。
- [ ] **臨時工作區（Ephemeral Workspace）**：敘事核心可在記憶體維護臨時狀態（劇情分支中間態、角色關係草稿、待審批任務草稿），不視為「第二套可寫圖譜」，但須滿足：
  - 必帶 `workspace_id`，並與 L0 圖譜的 `snapshot_id` 關聯
  - 生命週期不得超過當前任務
  - 寫入持久圖譜前必經顯式寫入 API（`/linkin/*` 或模組閘道）
  - L0 刷新導致快照變更時，臨時工作區**預設進入 `awaiting_confirmation`**（見 §9，保留用戶工作成果）；「丟棄」為顯式選項；僅在超過生命週期上限（當前任務結束）時才自動丟棄；禁止靜默沿用舊快照

### 4.8 前後端契約與對齊

- [ ] L0 狀態與三核結果透過**查詢／刷新 API** 暴露，與任務 token 串流解耦。
- [ ] 不在聊天完成時自動灌入分數或審計數字。
- [ ] 與公司運行時對齊：席位表、`docs/company/roles.md`、預算、暫停／審計狀態、模組閘道一致。

---

## 5. 後端強化：減少對大模型的倚賴　`P1`

### 5.1 功能定位

在不破壞指揮鏈、顯式審計契約與模組護欄的前提下，讓簡單／可判定路徑走規則、快取、模板與小模型，降低成本與延遲，並保持可測性。

### 5.2 路由與短路

- [ ] 強化 `route_by_complexity`／執行策略：簡單或可判定任務走規則、快取或模板路徑，避免一律進入完整反思閉環或完整公司運行時。
- [ ] 短路決策可觀測（路由原因碼），並可被測試 `monkeypatch`。

### 5.3 快取與重用

- [ ] 對相同／近似查詢、評估結果、角色 Prompt 片段、可安全重用的模組回應做**可觀測快取**（命中率、鍵策略、TTL／失效策略）。
- [ ] **分類型預設 TTL**（可配置）：角色 Prompt 片段長 TTL；評估結果短 TTL；審計／憲兵路徑預設旁路或 TTL=0；模組健康探測短 TTL。
- [ ] 命中時跳過或縮短 LLM 呼叫；快取鍵不得誤傷需新鮮上下文的審計／憲兵路徑（可配置旁路）。

### 5.4 啟發式評估

- [ ] 評估／反思節點優先使用規則、結構檢查、多維度門檻。
- [ ] **僅在邊界案例**才呼叫評估模型；禁止每輪必打大模型。

### 5.5 本地／小模型優先

- [ ] 經 `call_llm` 與模型池鎖定，預設路由至小模型／本地端點；**禁止**插件直連模型供應商 SDK。
- [ ] 大模型僅用於高複雜度或明確升級條件（策略表可配置）。
- [ ] **品質升級路徑**：小模型輸出經啟發式評估；不達標則升級至大模型，**有次數上限**；升級須寫入路由原因碼與成本觀測。

### 5.6 工具與確定性邏輯

- [ ] MCP／OPC／記憶檢索／預算與狀態機／模組閘道／編譯管線編排等由後端**確定性模組**完成。
- [ ] LLM 只負責需要生成的段落；禁止用模型代替可程式化決策（含是否暫停、是否審計、是否扣預算、是否啟用插件、是否部署編譯產物）。

### 5.7 觀測與配額

- [ ] 記錄每次請求的 LLM 呼叫次數、token、成本（對齊 `model_costs.json`），以及插件呼叫次數／延遲／錯誤碼、編譯失敗錯誤碼。
- [ ] 設軟上限與降級策略；超限時切規則／快取／停用外掛路徑，並對 UI／日誌可觀測。

### 5.8 測試與契約不變式

- [ ] 短路、快取、啟發式、模組路徑須可 `monkeypatch`，測試不依賴真實 API 或真實外掛遠端。
- [ ] **契約與 UI 不因減少 LLM 或接入插件而自動灌入審計分數／數字**；審計仍僅能由 §1 顯式流程觸發。

---

## 6. 插件整合：DeepSeek Club + dsh-plugin　`P2`

### 6.1 功能定位

在既有「世界模組」可插拔閘道（`backend/modules/`、`GET /modules`、`/{id}/api/*`）之上，納入兩類可選外掛來源，作為**可選、可開關**的能力擴充；不得破壞指揮鏈、顯式審計契約、L0 無工具直連，以及 MCP／OPC 護欄：

1. [DeepSeek Club 插件目錄](https://deepseek.club/plugins)
2. [GitHub `dsh-plugin` topic](https://github.com/topics/dsh-plugin) 下的開源／社群插件生態

兩者共用同一模組閘道與啟用契約；來源標籤可區分（`deepseek-club` / `dsh-plugin`），行為與護欄一致。

### 6.2 目錄與適配

- [ ] 盤點 `https://deepseek.club/plugins` 可用插件類型（工具、檢索、工作流、UI 擴充等），對照本專案模組契約產出適配表。
- [ ] 盤點 `https://github.com/topics/dsh-plugin` 相關倉庫／套件：能力類型、授權、依賴、安裝方式（npm／pip／git submodule 等）、與本專案模組契約的適配缺口；產出對照表與優先適配清單。
- [ ] 每個外掛（無論來源）對應一個 `backend/modules/` 條目（或子目錄），具備：模組 ID、顯示名、版本、**來源標籤**（`deepseek-club` | `dsh-plugin`）、能力聲明、依賴、預設關閉／需顯式啟用、倉庫／目錄 URL、**最高可掛載席位層級**。
- [ ] dsh-plugin 適配須定義：清單同步策略；**預設手動 pin 版本**；目錄刷新為可選且須顯式開啟；本地快取路徑、簽名／完整性校驗（若適用）、與現有 `GET /modules` 列表合併規則。
- [ ] 禁止插件直連模型供應商 SDK；LLM 一律經 `backend.core.llm.call_llm`。
- [ ] 禁止插件繞過協調器直連下層席位／工具；Minecraft／OPC 寫入仍須經既有護欄。

### 6.3 安全與邊界

- [ ] 插件安裝／啟用為**顯式動作**（API 或控制台），禁止串流完成或聊天 webhook 自動安裝／啟用；dsh-plugin 從 GitHub 拉取／更新同樣須顯式觸發。
- [ ] 工具範圍白名單：僅暴露模組契約允許的 API；禁止暴露 `write_file` 等檔案系統工具或任意 shell。
- [ ] 密鑰與外連：外掛憑證入密管／環境變數，面板與日誌不得洩漏；外連失敗可觀測並可降級。
- [ ] **預設降級策略**：外連／執行失敗時 **續跑＋告警**；關鍵路徑插件可配置為「暫停任務」；策略寫入模組契約與 UI。
- [ ] 供應鏈邊界（尤其 dsh-plugin）：僅允許白名單倉庫／已審核版本；禁止執行未審核遠端腳本；安裝產物不得寫入業務源碼樹外的任意路徑。
- [ ] 與審計解耦：插件不得自動灌入審計分數／等級；**審計範圍包含插件呼叫記錄**（參數摘要、來源標籤、成敗），但插件**不得**主動寫入審計結果。
- [ ] **降級不丟日誌**：插件降級／停用不得丟失已記錄的呼叫日誌；審計讀取的是**持久化軌跡**，與插件當前是否可用無關（對齊 §9 `plugin_degraded` 行允許審計）。

### 6.4 調度、預算與觀測

- [ ] 插件呼叫可歸因至席位／任務與**來源標籤**，納入預算扣減；超限時按 §5 降級（規則／快取／停用外掛路徑）。
- [ ] 席位表「可呼叫插件白名單」＋插件「最高可掛載層級」雙向校驗；不符則拒絕並可觀測。
- [ ] 記錄插件呼叫次數、延遲、錯誤碼、來源；路由原因碼可 `monkeypatch` 測試。
- [ ] UI（§3）可展示「外掛／模組」動作類型、來源（DeepSeek Club / dsh-plugin）與啟用狀態，與審計分數槽位分離。

### 6.5 文件與驗收

- [ ] 同步更新 `docs/structure.md`、模組 README／OpenAPI，並列出已適配的 DeepSeek Club 插件、dsh-plugin 倉庫／版本與開關預設。
- [ ] 驗收：未啟用時行為與現況一致；啟用後能力經模組閘道可達；繞過指揮鏈／護欄的路徑被拒絕；測試不依賴真實外掛遠端或真實 GitHub 拉取（可 stub／monkeypatch）。
- [ ] 與跨切面驗收交叉：席位表、暫停／審計狀態、L0 注入、降 LLM 路徑均不因任一插件來源整合而鬆綁。

---

## 7. 跨切面驗收清單（整合）

依破壞性標優先級；實作順序對齊總覽表。

### 7.1 功能契約　`P0–P2`

- [ ] **P0** 用戶未點「審計」時，全產品表面（含插件／模組／編譯回調）無審計分數／等級自動出現。
- [ ] **P0** 點「審計」：等待 inflight → `paused` → 審計報告；可恢復；狀態與 API 一致；失敗預設保持 `paused`。
- [ ] **P0** 指揮鏈席位可配置，調度必經協調器；L0 無工具／插件直連；繞過路徑被拒絕；席位表 `schema_version` 生效。
- [ ] **P0** MC 編譯／部署僅能顯式觸發；沙盒未通過不得部署；失敗保留舊版本、不強制 `/reload`（見 §8）。
- [ ] **P1** L0 三核可開關、可刷新、壓力驅動注入強度；敘事核心不雙寫世界觀；面板可下鑽且無密鑰洩漏；注入前脱敏。
- [ ] **P1** 後端複雜度路由／快取／啟發式／小模型優先生效，且有成本與呼叫次數觀測；小模型升級路徑有上限；測試可隔離。
- [ ] **P1** §9 狀態組合矩陣在 API／UI 一致；衝突路徑回傳明確錯誤碼而非靜默覆蓋。
- [ ] **P1** 審計軌跡持久化且只讀（§1.6）；插件降級／停用不丟失軌跡；快照雜湊輸入含插件 pin 版本（§1.5）。
- [ ] **P2** 對話介面同時正確展示：激活席位（主／從）、預算、活動／等待／暫停／審計中、動作類型、模組／外掛啟用狀態（含來源標籤）；預設折疊防過載。
- [ ] **P2** DeepSeek Club／dsh-plugin／世界模組：預設關閉；顯式啟用後經閘道可達；LLM 僅經 `call_llm`；工具白名單與護欄生效；外連／拉取失敗可降級；dsh-plugin 預設手動 pin。
- [ ] **P0–P2** `docs/company/roles.md`、席位表、預算、暫停／審計狀態、`docs/structure.md`／模組 OpenAPI 與實作同步（含雙來源插件清單與編譯器契約）。

### 7.2 效能驗收（補強）

- [ ] L0 `/refresh`（或等效）P95 耗時上限寫入契約（可配置；CI 可用 stub 時鐘／假資料驗證上限邏輯）。
- [ ] 插件啟用／停用 API 延遲上限與超時錯誤碼可觀測。
- [ ] 編譯管線（不含真實遊戲 I/O）在 stub 環境下有步驟級逾時與錯誤碼；不得無限掛起 UI。
- [ ] **預設 P95 上限參考值**（可配置，寫入契約檔）：

  | 動作 | 建議 P95 預設上限 |
  | --- | --- |
  | L0 `/refresh` | 3s |
  | 插件啟用／停用 API | 2s |
  | 編譯管線（stub 環境） | 5s |
  | 審計（不含 LLM 呼叫） | 1s |

### 7.3 測試隔離（不變）

- [ ] 測試不依賴真實外掛遠端、真實 GitHub 拉取、真實 MineMCP／遊戲伺服器或真實大模型 API。

---

## 8. Minecraft 適配器／編譯器契約　`P0`（新增）

### 8.1 功能定位

將「敘事／建築／內容產出 → Minecraft 可部署產物（schematic、資料包、功能組件／Mod 適配層）」納入與審計同級的**顯式治理**：編譯與部署皆為按需動作，禁止敘事核心或串流完成自動部署。對齊既有護欄：`backend/tools/minecraft_mcp.py`、`backend/linkin/minecraft.py`、`backend/linkin/schematic.py`；禁止角色直連 MineMCP，禁止暴露檔案系統寫入工具。

### 8.2 顯式動作與部署閘門

- [ ] **編譯**為顯式 API／控制台動作（例：`POST .../compile`）；敘事核心、聊天完成、插件回調皆**禁止**自動觸發編譯或部署。
- [ ] **部署**為獨立顯式動作；編譯成功 ≠ 自動部署。
- [ ] 編譯產物必須經**沙盒驗證**（結構校驗、版本相容、衝突掃描）通過後，才允許進入「待部署」狀態；**衝突掃描**至少涵蓋：
  - 命名空間衝突（`datapacks/[namespace]/` 是否已存在同名檔案）
  - 實體 ID 衝突（自訂實體是否與既有 ID 重複）
  - 配方衝突（合成表是否與既有配方衝突）
  - 戰利品表衝突
  - 標籤（Tag）覆蓋衝突
- [ ] 管理員／授權席位（席位表「部署確認權」為 `true` 者，見 §2.2；預設僅 L5＋人類管理員）確認前，禁止寫入遊戲世界或覆蓋線上資料包；確認動作可審計。

### 8.3 版本化、回滾與衝突

- [ ] 編譯產物帶版本號／內容雜湊；提供回滾 API（切回上一穩定版本）。
- [ ] 同名組件／資料包衝突時：拒絕覆蓋並回傳衝突清單；仲裁須顯式（合併策略或人工選擇），禁止靜默最後寫入勝出。
- [ ] 編譯失敗或沙盒失敗：**保留舊版本**，**禁止**自動 `/reload` 或強制熱更新；錯誤碼進入 §5 可觀測體系。

### 8.4 與指揮鏈／L0／審計的對齊

- [ ] 編譯／部署工作項只經協調器下發；插件與敘事席不得繞過。
- [ ] 編譯輸入若依賴世界觀狀態，僅消費 L0 注入／唯讀投影（§4.7），禁止編譯器私自改圖譜。
- [ ] 編譯／部署呼叫記錄納入審計範圍（與 §6.3 同構：可被審計讀取，不得主動灌入審計分數）。
- [ ] UI 動作類型區分「編譯中／沙盒驗證中／待部署／已回滾」；不與審計分數槽位混用。

### 8.5 文件與驗收

- [ ] 同步 `docs/linkin/minecraft-mcp.md`、`docs/structure.md`、模組 OpenAPI：編譯／部署／回滾端點、錯誤碼、沙盒狀態機。
- [ ] 驗收：未顯式編譯／部署時世界不變；失敗保留舊版；繞過護欄路徑被拒絕；測試以 stub schematic／假沙盒完成，不依賴真實伺服器。

### 8.6 編譯產物形態優先級（寫死）

- [ ] 產物形態優先級：**資料包（Datapack）＋資源包（Resource Pack）為首選**；**Schematic 結構檔**用於建築；**Java Mod 適配層僅允許預編譯的橋接層**。
- [ ] **禁止運行時生成／編譯 Java 代碼**：運行時編譯需 Gradle／JDK 環境、無法熱加載，且 AI 生成代碼崩潰風險極高；任何「生成 `.java` → 編譯 → 部署」路徑在 CI／運行時守衛中拒絕。

---

## 9. 狀態組合矩陣（暫停／審計／刷新／降級／編譯）　`P1`（新增）

定義可疊加狀態與禁止組合，避免實作各自解讀。符號：`✓` 允許、`✗` 拒絕（回傳衝突錯誤碼）、`Q` 可佇列待確認、`→` 強制順序。

> **狀態粒度聲明**：本矩陣以**單任務視角**描述；全局狀態（如插件全局降級、L0 全局刷新）與任務狀態的組合規則另文定義，實作時不得預設共享或隔離。

| 當前 \\ 請求 | pause | audit | resume | L0 刷新 | 插件啟用／停用 | 編譯 | 部署 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `running` | ✓（等 inflight） | → 先 pause 再 audit | ✗ | Q 或拒絕（策略可配；預設 Q） | ✓（不中斷 inflight 則鎖 UI） | ✓ | ✗（須編譯通過） |
| `paused` | — | ✓ → `auditing` | ✓（校驗快照版本） | Q（恢復前須確認版本） | ✓ | ✓ | ✓（若已待部署） |
| `auditing` | — | ✗ 重複 | ✗（審計結束前） | ✗（凍結快照） | ✗（凍結插件集合） | ✗ | ✗ |
| `l0_refreshing` | ✓ 可暫停任務 | Q（刷新完成後自動進入 `paused` → `auditing`） | ✗ | ✗ 重複 | Q | Q | ✗ |
| `plugin_degraded` | ✓ | ✓（範圍含插件呼叫記錄） | ✓ | ✓ | ✓（可重試啟用） | ✓ | 依策略 |
| `compiling` / `sandboxing` | ✓ | ✗ | ✗ | Q | Q | ✗ 重複 | ✗ |
| `awaiting_deploy` | ✓ | ✓ | ✓ | Q | ✓ | ✓（新編譯替換待部署） | ✓ 顯式 |
| `awaiting_confirmation`（快照衝突待確認） | — | ✗ | Q（須先確認衝突處理） | Q | Q | Q | ✗ |

### 9.1 預設策略（寫死）

- [ ] **審計中**：禁止 L0 刷新與插件集合變更；請求回傳衝突碼，可改為「審計結束後提示用戶」。
- [ ] **`awaiting_confirmation`**：由快照版本衝突（§1.5）觸發；UI 必須展示衝突詳情（快照差異摘要），用戶顯式選擇「以新上下文重綁」或「放棄任務」後才能離開此狀態；此狀態下禁止審計與部署。
- [ ] **插件降級中＋任務暫停**：恢復時**不自動重試**失敗插件；須顯式「重試啟用」或接受降級續跑。
- [ ] **編譯失敗**：狀態回到可操作（如 `paused` 或 `running` 依觸發前狀態），舊產物保留；不得進入半部署狀態。
- [ ] **編譯中禁止審計**：`compiling`／`sandboxing` 下審計一律拒絕（簡化實作，不分審計範圍）；編譯結束後可審計**包含編譯記錄的完整軌跡**（審計範圍含編譯／部署呼叫記錄，見 §8.4）。
- [ ] **`awaiting_deploy` 下 `pause` 語義（寫死）**：暫停的是**任務生命週期**；部署審批流程獨立存在，不受任務暫停影響——授權席位仍可在任務暫停期間確認／拒絕部署。
- [ ] 所有 `✗` 路徑必須：可觀測錯誤碼＋UI 提示＋不改變既有凍結快照。

### 9.2 驗收

- [ ] 矩陣中每個 `✗`／`Q` 至少一則 API 或狀態機單元測試。
- [ ] UI 按鈕可用性與矩陣一致，禁止前端自行放行後端會拒絕的組合。

---

## 附錄：契約抽出建議

- [ ] 將本檔「禁止／必須」條款抽出為 `CONTRACT.md`（或 `docs/contracts/runtime.md`），每條對應：pytest 不變式 ID 或靜態檢查規則 ID。
- [ ] `CONTRACT.md` 條款格式（寫死，供 CI 解析；每條契約可映射到具體測試／lint）：

  ````markdown
  ## CONTRACT-ID: C-AUDIT-001
  - **條款**: 禁止在串流 done 事件中自動灌入審計分數
  - **來源**: TODO §1.4
  - **優先級**: P0
  - **驗證**: pytest::test_no_auto_audit_score / lint::no_audit_score_in_stream
  - **狀態**: ✅ 已實現 / 🚧 進行中 / ❌ 未實現
  ````

- [ ] CI 門檻：P0 契約測試失敗則阻断合併；P1／P2 可分階段開啟。
